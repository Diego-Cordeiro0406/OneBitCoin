import { StyleSheet } from "react-native";

const styles = StyleSheet.create({
  currentContainer: {
    alignItems: 'center',
    height: 'auto',
    marginBottom: 20,
    width: '100%',
  },
  currentPrice: {
    color: '#FFFFFF',
    fontSize: 34,
    fontWeight: 'bold',
  },
  currentText: {
    color: '#FFFFFF',
    fontSize: 16,
    fontWeight: 'bold',
  }
})

export default styles